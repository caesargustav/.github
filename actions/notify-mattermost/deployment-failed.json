{
  "attachments": [
    {
      "fallback": "{{ github.repository }} deployment failed.",
      "color": "#ef4444",
      "text": "{{ github.repository }} deployment failed.  [Failed job]({{ github.job_url }})"
    }
  ]
}